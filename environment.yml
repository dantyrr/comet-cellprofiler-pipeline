name: comet-pipeline
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pandas>=1.5
  - numpy>=1.23
  - tifffile>=2023.2.28   # only needed for the OME-XML channel-verification snippet in docs/
