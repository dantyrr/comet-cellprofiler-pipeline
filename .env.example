COMET_PROJECT_ROOT=/path/to/your/project
SLURM_PARTITION=your_partition_name

# Only needed for tiling/tile.sh (local pre-processing step, not part of
# the Slurm pipeline). Defaults in the script already match dtyrrell's Mac
# setup, so these are optional overrides.
# COMET_BOX_DATA_ROOT=/path/to/Box/Nick's Comet data
# MAKE_TILES_SCRIPT=/path/to/make_tiles.py
# COMET_TILES_OUTPUT_ROOT=/path/to/tiles/output/root
